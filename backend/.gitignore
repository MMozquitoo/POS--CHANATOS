node_modules/
.env
data/*.db
data/*.db-journal
data/*.db-wal
# IMPORTANTE: products.json debe incluirse en Git (NO ignorarlo)
# !data/products.json
# Credenciales de cuenta de servicio de Firebase (push notifications) — secreto, nunca a Git
data/firebase-service-account.json
*.log
.DS_Store

